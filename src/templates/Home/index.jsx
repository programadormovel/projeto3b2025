import { Link } from "react-router-dom"

	const Home = () => {

	  return (
	    <div className="container">
	        <Link to={'/'} className="btn btn-info">Voltar</Link>
	    </div>
	  )
	}

	export default Home